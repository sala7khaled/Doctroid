<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="stToast">
        <item name="stIconStart">@drawable/icon_noback</item>
        <item name="stColorBackground">@color/colorPrimaryDark</item>
        <item name="stTextColor">@color/colorWhite</item>
        <item name="stTextSize">14sp</item>
        <item name="stFont">@font/gotham_rounded_medium</item>
        <item name="stStrokeColor">@color/colorPrimaryDark</item>
        <item name="stStrokeWidth">0dp</item>
        <item name="stLength">LONG</item>
        <item name="stRadius">30dp</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/back_top_corner</item>
    </style>

</resources>
